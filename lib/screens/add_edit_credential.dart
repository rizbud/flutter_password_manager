import 'package:flutter/material.dart';

class AddEditCredential extends StatelessWidget {
  const AddEditCredential({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Add/Edit Credential')),
      body: const Center(child: Text('Add/Edit Credential Screen')),
    );
  }
}
